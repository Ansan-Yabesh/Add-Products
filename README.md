# Express.js Add Product  

This is an Add Product project 🛒 built on Express.js — fast, minimal, and without a database.  
Serve static files, handle GET/POST, and launch in seconds with `npm install` → `node app.js` 🚀.  
